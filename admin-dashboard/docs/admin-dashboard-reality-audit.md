# Admin Dashboard Reality Audit (Updated)

This document reflects the verified state of all Admin Dashboard pages following the real API integration.

## Integration Overview Matrix

| Page | Displayed Data | Data Source | API Endpoint | Query Hook / Mechanism | Mutation Hook / Function | Validation | Loading State | Empty State | Error State | Permission Enforcement | Production Mock Status | Real Integration Status |
|---|---|---|---|---|---|---|---|---|---|---|---|---|
| **Overview** (`src/app/page.tsx`) | Total Requests, Successes, Failures, Costs, Health Summary | Backend API via SWR | `GET /admin/intelligent-services/usage`, `GET /admin/intelligent-services/health` | `useSWR('/admin/intelligent-services/usage')`, `useSWR('/admin/intelligent-services/health')` | None (Read-only Overview) | Response DTO | `Loader2` spinner | "لا توجد بيانات متاحة" | Red alert box | Middleware + Backend 401/403 | 0% Mock (100% Real API) | `CONNECTED_NOT_LIVE_TESTED` |
| **Providers** (`src/app/providers/page.tsx`) | Providers list, internal name, type, Base URL, masked secret, status | Backend API via SWR | `GET /admin/intelligent-services/providers`, `POST/PATCH/DELETE /admin/intelligent-services/providers`, `POST .../test` | `useSWR('/admin/intelligent-services/providers')` | `api.post()`, `api.patch()`, `api.delete()`, `api.post(.../test)` | Form validation (required key, name, baseUrl), Secret masking | `Loader2` spinner | "لا يوجد مزوّدين حالياً" | Red alert banner + Error toast | Middleware + Backend 401/403 | 0% Mock (100% Real API) | `CONNECTED_NOT_LIVE_TESTED` |
| **Models** (`src/app/models/page.tsx`) | Models list, remote model ID, provider binding, context window, capabilities | Backend API via SWR | `GET/POST/PATCH/DELETE /admin/intelligent-services/models` | `useSWR('/admin/intelligent-services/models')`, `useSWR('/admin/intelligent-services/providers')` | `api.post()`, `api.patch()`, `api.delete()` | DTO field checks (contextWindow, maxOutputTokens, numeric) | `Loader2` spinner | "لا يوجد نماذج حالياً" | Red alert banner | Middleware + Backend 401/403 | 0% Mock (100% Real API) | `CONNECTED_NOT_LIVE_TESTED` |
| **Routing** (`src/app/routing/page.tsx`) | Task routing policies, primary model, reorderable fallback candidates | Backend API via SWR | `GET /admin/intelligent-services/routes`, `PATCH /admin/intelligent-services/routes/:id` | `useSWR('/admin/intelligent-services/routes')`, `useSWR('/admin/intelligent-services/models')` | Drag & Drop (`@hello-pangea/dnd`) + `api.patch()` | Candidate priority reordering array validation | `Loader2` spinner | "لا يوجد سياسات توجيه مهام حالياً" | Red alert banner + rollback toast | Middleware + Backend 401/403 | 0% Mock (100% Real API) | `CONNECTED_NOT_LIVE_TESTED` |
| **Prompts** (`src/app/prompts/page.tsx`) | Prompt versions, system prompt, developer prompt, active version status | Backend API via SWR | `GET/POST /admin/intelligent-services/prompts`, `POST /admin/intelligent-services/prompts/:id/activate` | `useSWR('/admin/intelligent-services/prompts')` | `api.post()`, `api.post(.../activate)` | DTO validation (key regex, systemPrompt length) | `Loader2` spinner | "لا يوجد قوالب تعليمات حالياً" | Red alert banner | Middleware + Backend 401/403 | 0% Mock (100% Real API) | `CONNECTED_NOT_LIVE_TESTED` |
| **Knowledge Bases** (`src/app/knowledge/page.tsx`) | Knowledge bases list, scope, KEYWORD retrieval mode, test search excerpts | Backend API via SWR | `GET/POST /admin/knowledge-bases`, `POST /admin/knowledge-bases/:id/test-search` | `useSWR('/admin/knowledge-bases')` | `api.post()`, `api.post(.../test-search)` | Search query min length validation | `Loader2` spinner | "لا يوجد قواعد معرفة حالياً" | Red alert banner | Middleware + Backend 401/403 | 0% Mock (100% Real API) | `CONNECTED_NOT_LIVE_TESTED` |
| **Documents** (`src/app/documents/page.tsx`) | Document list per KB, status (READY, PROCESSING, FAILED, OCR_DISABLED), upload progress | Backend API via SWR | `GET/POST /admin/knowledge-bases/:id/documents`, `POST /admin/knowledge-documents/:id/reprocess`, `DELETE ...` | `useSWR('/admin/knowledge-bases/:id/documents')` | Multipart `FormData` `api.post()`, `api.post(.../reprocess)`, `api.delete()` | File type/size checks (PDF, TXT, DOCX), Multipart upload progress | `Loader2` spinner + Upload % bar | "لا يوجد مستندات في هذه القاعدة" | Red alert banner + failure reason | Middleware + Backend 401/403 | 0% Mock (100% Real API) | `CONNECTED_NOT_LIVE_TESTED` |
| **Usage Policies** (`src/app/usage-policies/page.tsx`) | Daily/Monthly user limits, global limit, max input/output tokens, cooldown | Backend API via SWR | `GET /admin/intelligent-services/usage-policies`, `PATCH /admin/intelligent-services/usage-policies/:taskType` | `useSWR('/admin/intelligent-services/usage-policies')` | `api.patch()` | Numeric non-negative validation, cooldown range | `Loader2` spinner | "لا يوجد سياسات مسجلة" | Red alert banner | Middleware + Backend 401/403 | 0% Mock (100% Real API) | `CONNECTED_NOT_LIVE_TESTED` |
| **Usage Stats** (`src/app/usage-stats/page.tsx`) | Total requests, successes, failures, estimated cost, cost breakdown per provider | Backend API via SWR | `GET /admin/intelligent-services/usage`, `GET /admin/intelligent-services/costs` | `useSWR('/admin/intelligent-services/usage')`, `useSWR('/admin/intelligent-services/costs')` | Read-only reporting | Numeric formatting | `Loader2` spinner | "لا توجد بيانات متاحة" | Red alert banner | Middleware + Backend 401/403 | 0% Mock (100% Real API) | `CONNECTED_NOT_LIVE_TESTED` |
| **Health** (`src/app/health/page.tsx`) | Overall system health status, circuit breaker states (CLOSED/OPEN) per component | Backend API via SWR | `GET /admin/intelligent-services/health` | `useSWR('/admin/intelligent-services/health')` | `mutate()` manual refresh | Health DTO format | `Loader2` spinner | "لا تتوفر تفاصيل إضافية للمكونات" | Red alert banner | Middleware + Backend 401/403 | 0% Mock (100% Real API) | `CONNECTED_NOT_LIVE_TESTED` |

## Detailed File Audits

### 1. Authentication Layer
- **Client Login Page**: `src/app/login/page.tsx`
- **Server Action**: `src/app/actions/auth.ts` (`loginAction`, `logoutAction`)
- **API Proxy Route**: `src/app/api/proxy/[...proxy]/route.ts`
- **Middleware**: `src/middleware.ts`
- **Axios Base Client**: `src/lib/axios.ts`
- **Token Storage**: `HttpOnly` Cookies (`admin_access_token`, `admin_refresh_token`). Zero token stored in `localStorage` or `sessionStorage` or exposed in React state or HTML DOM.

### 2. Mock Scan Findings
- All static mock arrays (`useState([ ... ])`) removed from production pages.
- No `setTimeout`, `Promise.resolve`, or `Math.random` simulated delays.
- Drag and drop in `src/app/routing/page.tsx` uses real persistence via `@hello-pangea/dnd` and `api.patch('/admin/intelligent-services/routes/:id', { candidates })`.
